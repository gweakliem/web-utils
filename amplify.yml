version: 1
settings:
  nodeVersion: "20"
frontend:
    phases:
        preBuild:
            commands:
                - 'corepack enable'
                - 'npm i'
        build:
            commands:
                - 'npm run build'
    artifacts:
        baseDirectory: .amplify-hosting
        files:
            - '**/*'
